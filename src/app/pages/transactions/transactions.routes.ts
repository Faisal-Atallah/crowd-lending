import { Routes } from "@angular/router";
import { TransactionsListComponent } from "./list";

export default [] = [
    {
        path: '',
        component: TransactionsListComponent
    }
] as Routes;